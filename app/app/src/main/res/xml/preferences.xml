<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditTextPreference android:key="server_url" android:title="Server URL"
        android:defaultValue="http://192.168.1.100:8080" android:inputType="textUri"
        app:useSimpleSummaryProvider="true" />

    <EditTextPreference android:key="api_key" android:title="API Key"
        android:inputType="textPassword" android:summary="Tap to set API key" />

    <SwitchPreferenceCompat android:key="wifi_only" android:title="WiFi only uploads"
        android:summary="Only upload when connected to WiFi" android:defaultValue="true" />

    <SeekBarPreference android:key="session_gap_minutes" android:title="Session gap (minutes)"
        android:summary="Silence that triggers a new session"
        android:defaultValue="2" android:min="1" android:max="10" />

    <SeekBarPreference android:key="chunk_length_s" android:title="Chunk length (seconds)"
        android:summary="Length of each uploaded audio chunk"
        android:defaultValue="60" android:min="30" android:max="120" />

</PreferenceScreen>
